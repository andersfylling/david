add_subdirectory(Catch)     # Catch, for unit testing.
add_subdirectory(fann)      # Fann, for faster neural network development.
add_subdirectory(benchmark) # Benchmark, google benchmarks for easier decision making and overview of code performance.
add_subdirectory(uci)       # UCI, to handle incoming and outgoing commands between GUI and ENGINE.